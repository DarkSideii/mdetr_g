# --- Core numerical / DL ---
numpy==2.2.6
torch==2.4.1+cu121
torchvision==0.19.1+cu121

# --- Transformers (text encoder) ---
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.30.2
datasets==3.6.0

# --- Vision backbones ---
timm==1.0.19
satlaspretrain-models==0.3.1

# --- Deformable attention (MMCV op) ---
mmengine==0.10.7
openmim==0.3.9
# IMPORTANT: do NOT pip install mmcv from PyPI for deformable attention.
# Install MMCV with CUDA ops like this:
#   mim install "mmcv==2.2.0" -f https://download.openmmlab.com/mmcv/dist/cu121/torch2.4/index.html

# --- Data / storage / cache ---
boto3==1.34.162
pyarrow==21.0.0
rasterio==1.4.3
affine==2.4.0
tqdm==4.66.5
Pillow==11.2.1

# --- Eval / metrics ---
pycocotools==2.0.10
scikit-learn==1.7.1

# --- Logging / tracking ---
mlflow==3.3.2

# --- Dataset pipeline / env ---
python-dotenv==1.1.1
openai==1.102.0
