# =========================
# DGX Spark (CUDA 13.0) + Py3.11
# =========================
#
# PyTorch: install from the cu130 index:
#   pip install --index-url https://download.pytorch.org/whl/cu130 \
#     torch==2.9.1+cu130 torchvision==0.24.1 torchaudio==2.9.1
#
# MMCV: built from source with CUDA ops (editable) in ./mmcv
#   DO NOT install mmcv from pip here, or you may overwrite the built ops.

# --- Core numerical / DL ---
numpy==2.2.6
torch==2.9.1+cu130
torchvision==0.24.1
torchaudio==2.9.1

# --- Transformers (text encoder) ---
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.30.2
datasets==3.6.0

# --- Vision backbones ---
timm==1.0.19
satlaspretrain-models==0.3.1

# --- MMCV ecosystem ---
mmengine==0.10.7
# mmcv==2.2.0  # installed from local source with CUDA ops in ./mmcv

# --- Data / storage / cache ---
boto3==1.34.162
pyarrow==21.0.0
affine==2.4.0
tqdm==4.66.5
Pillow==11.2.1

# --- Eval / metrics ---
pycocotools==2.0.10
scikit-learn==1.7.1

# --- Logging / tracking ---
mlflow==3.3.2

# --- Dataset pipeline / env ---
python-dotenv==1.1.1
openai==1.102.0
