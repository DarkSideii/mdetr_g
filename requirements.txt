# --- Core numerical / DL ---
numpy==2.2.6
torch==2.4.1
torchvision==0.19.1

# --- Transformers (text encoder) ---
transformers==4.46.3
tokenizers==0.20.3
huggingface-hub==0.30.2
datasets==3.6.0

# --- Vision backbones ---
timm==1.0.19
satlaspretrain-models==0.3.1

# --- Deformable attention (MMCV op) ---
mmcv==2.2.0
mmengine==0.10.7
# (Install a CUDA-matching wheel for mmcv; openmim is optional and not required at runtime.)

# --- Data / storage / cache ---
boto3==1.34.162
pyarrow==21.0.0
rasterio==1.4.3
affine==2.4.0
tqdm==4.66.5
Pillow==11.2.1

# --- Eval / metrics ---
pycocotools==2.0.10
scikit-learn==1.7.1

# --- Logging / tracking ---
mlflow==3.3.2

# --- Dataset pipeline / env ---
python-dotenv==1.1.1
openai==1.102.0